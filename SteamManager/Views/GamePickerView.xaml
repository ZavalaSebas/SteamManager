<UserControl x:Class="SteamManager.Views.GamePickerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:SteamManager.Controls"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="#1A1A1A">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="My Games"
                       FontSize="32"
                       FontWeight="Light"
                       Foreground="#FFFFFF"
                       FontFamily="Segoe UI"/>
            <TextBlock Text="{Binding Games.Count, StringFormat='{}{0} games in library'}"
                       FontSize="13"
                       Foreground="#6B6B6B"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Search Bar -->
        <Border Grid.Row="1"
                Margin="0,0,0,20"
                Background="#252525"
                CornerRadius="8"
                BorderBrush="#3A3A3A"
                BorderThickness="1">
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="BorderBrush" Value="#4A4A4A"/>
                        </Trigger>
                        <DataTrigger Binding="{Binding IsFocused, ElementName=SearchBox}" Value="True">
                            <Setter Property="BorderBrush" Value="#66C0F4"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid Margin="14,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="&#xE721;"
                           FontFamily="Segoe MDL2 Assets"
                           FontSize="16"
                           Foreground="#6B6B6B"
                           VerticalAlignment="Center"
                           Margin="0,0,12,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsFocused, ElementName=SearchBox}" Value="True">
                                    <Setter Property="Foreground" Value="#66C0F4"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <TextBox x:Name="SearchBox"
                         Grid.Column="1"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource SearchTextBoxStyle}"
                         FontSize="14"
                         CaretBrush="#66C0F4"/>

                <TextBlock Grid.Column="1"
                           Text="Search games..."
                           Foreground="#5A5A5A"
                           FontSize="14"
                           VerticalAlignment="Center"
                           IsHitTestVisible="False"
                           Margin="2,0,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <Button Grid.Column="2"
                        Visibility="{Binding SearchText, Converter={StaticResource StringToVisibilityConverter}}"
                        Click="ClearSearch_Click"
                        Background="Transparent"
                        BorderThickness="0"
                        Cursor="Hand"
                        Padding="4">
                    <TextBlock Text="&#xE711;"
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="12"
                               Foreground="#6B6B6B"/>
                </Button>
            </Grid>
        </Border>

        <!-- Loading Skeleton -->
        <Border Grid.Row="2"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <ScrollViewer VerticalScrollBarVisibility="Disabled">
                <WrapPanel>
                    <!-- Skeleton Cards (8 placeholders) -->
                    <Border Width="200" Height="170" Margin="4" CornerRadius="8" Background="#252525">
                        <Border.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                        From="0.5" To="1" Duration="0:0:0.8"
                                                        AutoReverse="True"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Border.Triggers>
                        <StackPanel>
                            <Border Height="110" Background="#2A2A2A" CornerRadius="8,8,0,0"/>
                            <Border Height="3" Background="#3A3A3A" Margin="10,10,10,0" CornerRadius="2"/>
                            <Border Height="10" Width="120" Background="#2A2A2A" Margin="10,8,10,0" CornerRadius="2"/>
                        </StackPanel>
                    </Border>
                    <Border Width="200" Height="170" Margin="4" CornerRadius="8" Background="#252525">
                        <Border.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                        From="0.5" To="1" Duration="0:0:0.8"
                                                        BeginTime="0:0:0.1"
                                                        AutoReverse="True"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Border.Triggers>
                        <StackPanel>
                            <Border Height="110" Background="#2A2A2A" CornerRadius="8,8,0,0"/>
                            <Border Height="3" Background="#3A3A3A" Margin="10,10,10,0" CornerRadius="2"/>
                            <Border Height="10" Width="120" Background="#2A2A2A" Margin="10,8,10,0" CornerRadius="2"/>
                        </StackPanel>
                    </Border>
                    <Border Width="200" Height="170" Margin="4" CornerRadius="8" Background="#252525">
                        <Border.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                        From="0.5" To="1" Duration="0:0:0.8"
                                                        BeginTime="0:0:0.2"
                                                        AutoReverse="True"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Border.Triggers>
                        <StackPanel>
                            <Border Height="110" Background="#2A2A2A" CornerRadius="8,8,0,0"/>
                            <Border Height="3" Background="#3A3A3A" Margin="10,10,10,0" CornerRadius="2"/>
                            <Border Height="10" Width="120" Background="#2A2A2A" Margin="10,8,10,0" CornerRadius="2"/>
                        </StackPanel>
                    </Border>
                    <Border Width="200" Height="170" Margin="4" CornerRadius="8" Background="#252525">
                        <Border.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                        From="0.5" To="1" Duration="0:0:0.8"
                                                        BeginTime="0:0:0.3"
                                                        AutoReverse="True"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Border.Triggers>
                        <StackPanel>
                            <Border Height="110" Background="#2A2A2A" CornerRadius="8,8,0,0"/>
                            <Border Height="3" Background="#3A3A3A" Margin="10,10,10,0" CornerRadius="2"/>
                            <Border Height="10" Width="120" Background="#2A2A2A" Margin="10,8,10,0" CornerRadius="2"/>
                        </StackPanel>
                    </Border>
                    <Border Width="200" Height="170" Margin="4" CornerRadius="8" Background="#252525">
                        <Border.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                        From="0.5" To="1" Duration="0:0:0.8"
                                                        BeginTime="0:0:0.4"
                                                        AutoReverse="True"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Border.Triggers>
                        <StackPanel>
                            <Border Height="110" Background="#2A2A2A" CornerRadius="8,8,0,0"/>
                            <Border Height="3" Background="#3A3A3A" Margin="10,10,10,0" CornerRadius="2"/>
                            <Border Height="10" Width="120" Background="#2A2A2A" Margin="10,8,10,0" CornerRadius="2"/>
                        </StackPanel>
                    </Border>
                    <Border Width="200" Height="170" Margin="4" CornerRadius="8" Background="#252525">
                        <Border.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                        From="0.5" To="1" Duration="0:0:0.8"
                                                        BeginTime="0:0:0.5"
                                                        AutoReverse="True"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Border.Triggers>
                        <StackPanel>
                            <Border Height="110" Background="#2A2A2A" CornerRadius="8,8,0,0"/>
                            <Border Height="3" Background="#3A3A3A" Margin="10,10,10,0" CornerRadius="2"/>
                            <Border Height="10" Width="120" Background="#2A2A2A" Margin="10,8,10,0" CornerRadius="2"/>
                        </StackPanel>
                    </Border>
                    <Border Width="200" Height="170" Margin="4" CornerRadius="8" Background="#252525">
                        <Border.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                        From="0.5" To="1" Duration="0:0:0.8"
                                                        BeginTime="0:0:0.6"
                                                        AutoReverse="True"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Border.Triggers>
                        <StackPanel>
                            <Border Height="110" Background="#2A2A2A" CornerRadius="8,8,0,0"/>
                            <Border Height="3" Background="#3A3A3A" Margin="10,10,10,0" CornerRadius="2"/>
                            <Border Height="10" Width="120" Background="#2A2A2A" Margin="10,8,10,0" CornerRadius="2"/>
                        </StackPanel>
                    </Border>
                    <Border Width="200" Height="170" Margin="4" CornerRadius="8" Background="#252525">
                        <Border.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                        From="0.5" To="1" Duration="0:0:0.8"
                                                        BeginTime="0:0:0.7"
                                                        AutoReverse="True"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Border.Triggers>
                        <StackPanel>
                            <Border Height="110" Background="#2A2A2A" CornerRadius="8,8,0,0"/>
                            <Border Height="3" Background="#3A3A3A" Margin="10,10,10,0" CornerRadius="2"/>
                            <Border Height="10" Width="120" Background="#2A2A2A" Margin="10,8,10,0" CornerRadius="2"/>
                        </StackPanel>
                    </Border>
                </WrapPanel>
            </ScrollViewer>
        </Border>

        <!-- Games List -->
        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Visibility="{Binding IsLoading, Converter={StaticResource InverseBoolToVisibilityConverter}}">
            <ItemsControl ItemsSource="{Binding Games}"
                          VirtualizingStackPanel.IsVirtualizing="True"
                          VirtualizingStackPanel.VirtualizationMode="Recycling">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <controls:GameCard DataContext="{Binding}"
                                          GameSelected="GameCard_GameSelected"
                                          FavoriteToggle="GameCard_FavoriteToggle"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Status Bar -->
        <Border Grid.Row="3"
                Margin="0,16,0,0"
                Padding="0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding StatusMessage}"
                               FontSize="12"
                               Foreground="#6B6B6B"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                    <ProgressBar IsIndeterminate="True"
                                 Width="80"
                                 Height="4"
                                 Foreground="#66C0F4"
                                 Background="#3A3A3A"/>
                    <TextBlock Text="Loading..."
                               FontSize="11"
                               Foreground="#5A5A5A"
                               Margin="8,0,0,0"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
